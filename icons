# HG changeset patch
# User Brian Grinstead <bgrinstead@mozilla.com>
# Parent  8968d387064d001fe3a8eac583d9b48a26c58140
1193006 - Show icons next to permissions in main view;r=paolo

diff --git a/browser/base/content/browser.js b/browser/base/content/browser.js
--- a/browser/base/content/browser.js
+++ b/browser/base/content/browser.js
@@ -7360,20 +7360,26 @@ var gIdentityHandler = {
     let labelText = SitePermissions.getPermissionLabel(aPermission);
     label.setAttribute("flex", "1");
     label.setAttribute("class", "identity-popup-permission-label");
     label.setAttribute("control", menulist.getAttribute("id"));
     label.setAttribute("crop", "end");
     label.setAttribute("value", labelText);
     label.setAttribute("tooltiptext", labelText);
 
+    let image = document.createElement("image");
+    image.setAttribute("showing", "true");
+    image.setAttribute("class", "webRTC-shareDevices-notification-icon notification-anchor-icon");
+
     let container = document.createElement("hbox");
     container.setAttribute("align", "center");
+    container.appendChild(image);
     container.appendChild(label);
     container.appendChild(menulist);
+
     return container;
   }
 };
 
 function getNotificationBox(aWindow) {
   var foundBrowser = gBrowser.getBrowserForDocument(aWindow.document);
   if (foundBrowser)
     return gBrowser.getNotificationBox(foundBrowser)
