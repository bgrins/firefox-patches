# HG changeset patch
# User Brian Grinstead <bgrinstead@mozilla.com>
# Parent  aa8f530a1a35ba9b1c84303dbe15107d0026d77c

diff --git a/toolkit/content/tests/chrome/test_mousescroll.xul b/toolkit/content/tests/chrome/test_mousescroll.xul
--- a/toolkit/content/tests/chrome/test_mousescroll.xul
+++ b/toolkit/content/tests/chrome/test_mousescroll.xul
@@ -179,16 +179,20 @@ function* testArrowScrollbox(id)
                                 if (oldPos == getPos()) {
                                   // If scroll stopped completely, let's continue the test.
                                   return ++retry == MAX_RETRY_COUNT;
                                 }
                                 oldPos = getPos();
                                 retry = 0;
                                 return false;
                              });
+      if (expected == 272) {
+        console.log("match?", expected, aExpected, getPos());
+       // yield new Promise(r=>r);
+      }
       if (IS_MAC && expected == aExpected && expected != 0) {
         // XXX On macOS, <scrollbox>.scrollPosition += enoughInt; may not set the scrollPosition
         //     to scrollSize even if enoughInt is larger than scrollSize.
         todo_is(getPos(), expected,
                 "testArrowScrollbox(" + id +  "): vertical, starting " + aStart +
                   " delta " + aDelta + " lineOrPageDelta " + lineOrPageDelta +
                   " aDeltaMode " + aDeltaMode);
         let isInRange = false;
