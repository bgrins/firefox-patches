# HG changeset patch
# User Jan Odvarko <odvarko@gmail.com>
# Date 1420626303 -3600
#      Wed Jan 07 11:25:03 2015 +0100
# Node ID 11479849d7afc9ecfb59c3cb6f1bbdb49ad3b4e3
# Parent d350cd94c55177c0abe93c4f88aa76765d355cd7
Bug 1118179 - Assume light theme colors as the default; r=bgrins

diff --git a/browser/devtools/fontinspector/font-inspector.js b/browser/devtools/fontinspector/font-inspector.js
--- a/browser/devtools/fontinspector/font-inspector.js
+++ b/browser/devtools/fontinspector/font-inspector.js
@@ -93,18 +93,19 @@ FontInspector.prototype = {
         !this.inspector.selection.isConnected() ||
         !this.inspector.selection.isElementNode() ||
         this.chromeDoc.body.classList.contains("dim")) {
       return;
     }
 
     this.chromeDoc.querySelector("#all-fonts").innerHTML = "";
 
-    let fillStyle = (Services.prefs.getCharPref("devtools.theme") == "light") ?
-        "black" : "white";
+    // Assume light theme colors as the default (see also bug 1118179).
+    let fillStyle = (Services.prefs.getCharPref("devtools.theme") == "dark") ?
+        "white" : "black";
     let options = {
       includePreviews: true,
       previewFillStyle: fillStyle
     }
     let fonts = [];
     if (showAllFonts){
       fonts = yield this.pageStyle.getAllUsedFontFaces(options)
                       .then(null, console.error);
