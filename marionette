# HG changeset patch
# User Brian Grinstead <bgrinstead@mozilla.com>
# Parent  17116905bc072c37d74226ccc46c93f0bd45d516
Bug 1479125 - Update marionette driver to access tabBrowser.tabs as an iterable;r=paolo

diff --git a/testing/marionette/driver.js b/testing/marionette/driver.js
--- a/testing/marionette/driver.js
+++ b/testing/marionette/driver.js
@@ -257,22 +257,22 @@ Object.defineProperty(GeckoDriver.protot
   get() {
     let hs = [];
 
     for (let win of this.windows) {
       let tabBrowser = browser.getTabBrowser(win);
 
       // Only return handles for browser windows
       if (tabBrowser && tabBrowser.tabs) {
-        tabBrowser.tabs.forEach(tab => {
+        for (let tab of tabBrowser.tabs) {
           let winId = this.getIdForBrowser(browser.getBrowserForTab(tab));
           if (winId !== null) {
             hs.push(winId);
           }
-        });
+        }
       }
     }
 
     return hs;
   },
 });
 
 Object.defineProperty(GeckoDriver.prototype, "chromeWindowHandles", {
