# HG changeset patch
# User Andrew Krawchyk <akrawchyk@gmail.com>
# Parent  fcf23377f9c3015a884ad531cbab5598d09073a3
Bug 1189464 - Remove theme-fg-contrast class from closing tag in markup view after editing outer html;r=bgrins

diff --git a/devtools/client/markupview/markup-view.js b/devtools/client/markupview/markup-view.js
--- a/devtools/client/markupview/markup-view.js
+++ b/devtools/client/markupview/markup-view.js
@@ -1821,16 +1821,17 @@ MarkupContainer.prototype = {
           line.classList.add("tag-line");
 
           let tagState = this.markup.doc.createElement("div");
           tagState.classList.add("tag-state");
           line.appendChild(tagState);
 
           line.appendChild(closingTag.cloneNode(true));
 
+          flashElementOff(line);
           this.closeTagLine = line;
         }
         this.elt.appendChild(this.closeTagLine);
       }
 
       this.elt.classList.remove("collapsed");
       this.expander.setAttribute("open", "");
       this.hovered = false;
