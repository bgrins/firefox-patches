# HG changeset patch
# User Brian Grinstead <bgrinstead@mozilla.com>
# Parent  aa8f530a1a35ba9b1c84303dbe15107d0026d77c

diff --git a/toolkit/content/tests/chrome/test_mousescroll.xul b/toolkit/content/tests/chrome/test_mousescroll.xul
--- a/toolkit/content/tests/chrome/test_mousescroll.xul
+++ b/toolkit/content/tests/chrome/test_mousescroll.xul
@@ -196,16 +196,18 @@ function* testArrowScrollbox(id)
           isInRange = getPos() > aStart && getPos() < expected;
         else
           isInRange = getPos() < aStart && getPos() > expected;
         ok(isInRange,
            "testArrowScrollbox(" + id +  "): vertical, starting " + aStart +
              " delta " + aDelta + " lineOrPageDelta " + lineOrPageDelta +
              " aDeltaMode " + aDeltaMode);
       } else {
+
+
         is(getPos(), expected,
            "testArrowScrollbox(" + id +  "): vertical, starting " + aStart +
              " delta " + aDelta + " lineOrPageDelta " + lineOrPageDelta +
              " aDeltaMode " + aDeltaMode);
       }
     }
 
     scrollbox.scrollTo(aStart, aStart);
@@ -250,16 +252,21 @@ function* testArrowScrollbox(id)
            "testArrowScrollbox(" + id +  "): horizontal, starting " + aStart +
              " delta " + aDelta + " lineOrPageDelta " + lineOrPageDelta +
              " aDeltaMode " + aDeltaMode);
       } else {
         is(getPos(), expected,
            "testArrowScrollbox(" + id +  "): horizontal, starting " + aStart +
              " delta " + aDelta + " lineOrPageDelta " + lineOrPageDelta +
              " aDeltaMode " + aDeltaMode);
+        if (id == "vscrollbox") { // && orientIsHorizontal) { //} && aStart == 50 && aDelta == -1000 && lineOrPageDelta == -100 && aDeltaMode == 0) {
+          console.log(getPos(), expected);
+          yield new Promise(r=>r);
+          debugger;
+        }
       }
     }
   }
 
   var scrolledWidth = scrollbox.scrollWidth;
   var scrolledHeight = scrollbox.scrollHeight;
   var scrollMaxX = scrolledWidth - scrollbox.getBoundingClientRect().width;
   var scrollMaxY = scrolledHeight - scrollbox.getBoundingClientRect().height;
