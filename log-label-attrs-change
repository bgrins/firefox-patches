# HG changeset patch
# User Brian Grinstead <bgrinstead@mozilla.com>
# Parent  f78fd5bc3c93ce5f58f9b48b050ef42295e50ffc

diff --git a/toolkit/content/widgets/text.js b/toolkit/content/widgets/text.js
--- a/toolkit/content/widgets/text.js
+++ b/toolkit/content/widgets/text.js
@@ -43,16 +43,20 @@ class MozTextLabel extends MozXULTextEle
   static get observedAttributes() {
     return ["accesskey", "text"];
   }
 
   attributeChangedCallback(name, oldValue, newValue) {
     if (!this.isConnectedAndReady || oldValue == newValue) {
       return;
     }
+    if (this.closest("#updateButton")) {
+      // console.trace();
+      console.log(this, name, oldValue, newValue, this.closest("#updateButton").getAttribute("data-l10n-id"));
+    }
     if (name == "text") {
       this._lastFormattedAccessKey = null;
     }
     this.formatAccessKey();
   }
 
   _onClick(event) {
     let controlElement = this.labeledControlElement;
