diff --git a/taskcluster/taskgraph/templates/gecko-profile.yml b/taskcluster/taskgraph/templates/gecko-profile.yml
--- a/taskcluster/taskgraph/templates/gecko-profile.yml
+++ b/taskcluster/taskgraph/templates/gecko-profile.yml
@@ -3,7 +3,7 @@
 then:
     $if: task.extra["suite"]
     then:
-        $if: task.extra.suite["name"] in ["talos", "raptor"]
+        $if: task.extra.suite in ["talos", "raptor"]
         then:
             task:
                 # We can't use mergeDeep as that will append the command below

